{
  "name": "freeplaneweb",
  "version": "1.0.0",
  "description": "A tool to view and search existing freeplane mindmap documents from a web browser.",
  "main": "src/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild src/main.js --bundle --outfile=dist/bundle.js --format=esm",
    "dev": "esbuild src/main.js --bundle --outfile=dist/bundle.js --format=esm --servedir=./ --watch"
  },
  "keywords": [
    "freeplane",
    "freemind",
    "mindmap",
    "viewer"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "d3": "^7.9.0",
    "esbuild": "^0.25.5"
  }
}
